using UnityEngine;

public class EnemyAI : MonoBehaviour
{
    /*float attackRange = Vector3.Distance();

    // Update is called once per frame
    void Update()
    {
        float attackScore = attackRange * dotAngleToPlayer;

        float chaseScore = -attackRange * dotAngleToPlayer;

        float patrolScore = -dotAngleToPlayer;

        float bestMove = Mathf.Max(attackScore, chaseScore, patrolScore);


        if(bestMove == attackScore)
        {
            AttackPlayer();
        }
        else if(bestMove == chaseScore)
        {
            ChasePlayer();
        }
        else
        {
            Patrol();
        }
    }
    */
}
